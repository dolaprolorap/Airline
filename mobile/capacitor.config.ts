import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'airline.mobile',
  appName: 'airline-mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
